# Lint 清理后的运行时纠偏记录

日期：2026-09-15。复核对象：`28c84d2`（`chore/lint-warning-cleanup`）。

## 处理原则

- 保留类型、未使用变量及已确认安全的导入改进。
- 撤回仅用于绕过 lint 的延迟，修复有行为证据的状态和依赖问题。
- 不修改 `.eslint-baseline.json` 的原 845 条基线，不放宽规则或新增抑制。
- 按每文件、每规则检查预算；允许暂时保留需要后续设计重构的原有告警。

## 已完成的纠偏

- 漫画阅读：章节重置先于缓存历史恢复，避免同页数章节切换后回到第一页；先读取本地阅读设置，避免持久化 effect 提前覆盖已保存设置。
- 私人影库：源切换才重置分类，URL 中的分类变化不再触发源重置。
- ACG / Pansou：保留提交触发搜索的语义，输入或回调身份变化不额外提交；没有云盘过滤条件时也不会重复请求。
- AI 评论：切换影片立即撤销旧影片的生成权限，保留取消请求、过期响应保护和轮询。
- TV：修复账户对象快照不稳定导致的循环渲染，以及遥控设置选中状态未更新；无关 URL 参数变化不重置播放选集。
- 一起看：屏幕房间的普通状态更新不再重置房主心跳，保留本地画质设置。
- 后台和弹窗：恢复草稿、配置回填及动画的执行顺序；自动加载与手动扫描分开管理，取消或忽略过期请求；过滤配置读取期间禁止编辑和保存。
- 登录、注册、直播：恢复请求和配置初始化顺序，保留认证导航、QR 轮询清理及原有展示延迟。

对原提交涉及的源码使用 TypeScript AST 统计 `setTimeout` 调用，结果如下。移除的是 200 个新增零延迟调用，并非删除所有定时器。

| 调用类型                | 清理前 `28c84d2^` | 清理提交 `28c84d2` | 纠偏后 |
| ----------------------- | ----------------: | -----------------: | -----: |
| 全部 `setTimeout`       |               163 |                363 |    163 |
| 显式零延迟 `setTimeout` |                12 |                212 |     12 |

## 保持不变的部分

三个配置 hooks 的 SSR 默认值与旧版 `useState` 初值一致。新增测试验证：SSR 使用默认值，水合后读取实际配置，默认和非默认配置均无可恢复水合错误。这不表示默认首屏已经消除，也不表示实现了配置热更新；这些 hooks 仍按当前文档的静态配置使用。

没有证据证明首页、图书守卫或 QR 的导航改动导致所述回归，因此保留当前导航方式。首页测试补齐 App Router 测试环境，没有删除或放宽原有业务断言。

## 验证

- `pnpm lint`：0 errors、182 warnings，逐文件/规则均不超原基线。
- `pnpm typecheck`：通过。
- `pnpm exec jest --runInBand --silent --noStackTrace`：95 套件通过，2 套件跳过；1366 测试通过，23 跳过。
- `git diff --check`：通过。6 个新增测试文件的 Prettier 检查通过。

新增测试文件：

- `tests/manga-reader-position.test.js`
- `tests/private-library-selection.test.js`
- `tests/lint-component-effects.test.js`
- `tests/admin-lint-effects.test.js`
- `tests/lint-page-effects.test.js`
- `tests/runtime-config-hooks-hydration.test.js`

这次使用实际组件的行为回归测试确认问题和修复，不以 lint 清零作为运行时正确性的证据。未完成真实浏览器、TV 设备端到端验收，也未执行完整生产构建。临时本地开发服务已停止。
